# Lunera UI Rules

- Use native SwiftUI Liquid Glass for app buttons and tappable pill controls. Keep custom flat fills only as an availability fallback when native glass APIs are unavailable.
- The canonical design guidance now lives in `Docs/DESIGN_SYSTEM.md`. Keep this file as a short compatibility note for older tasks that reference it.
